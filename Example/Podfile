use_frameworks!

target 'PanelController_Example', :exclusive => true do
  pod 'PanelController', :path => '../'
end

target 'PanelController_Tests', :exclusive => true do
  pod 'PanelController', :path => '../'

  pod 'Quick', '~> 0.8'
  pod 'Nimble', '~> 3.0'
  pod 'FBSnapshotTestCase'
  pod 'Nimble-Snapshots'
end
